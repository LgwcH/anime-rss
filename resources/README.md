# AniRSS 资源目录

- `icons/`：可选应用图标位置；正式图标尚未提供。
- `licenses/`：发布包所含第三方资源和二进制的许可证/来源记录。

0.1 的运行设置保存在 SQLite 中，且尚不支持 OPML 导入，因此这里不提供会被误认为可导入的
TOML 或 OPML 示例。

不要把真实 RSS URL、Tracker passkey、Cookie、Token、数据库、日志或下载内容放进此目录。
新增图片、字体或图标时，请在 `licenses/` 中记录文件名、作者、来源、许可证及修改情况，并确认
许可证允许在 MIT 项目和二进制发行包中再分发。
