# AniRSS 0.1.2 发布说明

发布日期：2026-08-09  
状态：Alpha  
桌面发行包：Windows x86_64，包含 BT/libtorrent

AniRSS 是一个本地运行的开源 RSS/Atom 订阅与下载管理器。它按每个订阅识别新条目，将 HTTP
文件、种子文件或磁力链接交给内置下载器，并保存到自动创建或用户指定的番剧目录。

## 本次更新

0.1.2 重新设计了桌面界面，并重点解决 Windows 显示缩放、长文本和小屏幕布局问题：

- 统一浅色与深色主题的色彩、卡片、表格、输入框和导航层级；
- 主窗口根据当前显示器可用区域确定尺寸，窄屏自动切换紧凑侧栏；
- 概览统计卡、下载列、设置行和订阅详情会按可用宽高重排；
- 番剧名、保存路径、任务标题、速度和时间采用有界显示，省略内容可通过提示查看；
- 极矮窗口中，条目详情切换为独占可滚动视图，避免同时挤压列表与详情；
- 筛选无结果与真正无数据使用不同空状态；
- 新增可中断的页面淡入淡出、侧栏选中指示和详情过渡；
- 启用 Windows“关闭动画效果”或设置 `ANIRSS_REDUCE_MOTION=1` 时，界面直接完成状态切换；
- 搜索、下载进度、速度刷新和主题切换保持即时，不添加会妨碍读取的高频动画。

订阅详情和逐条自主下载功能继续保留：点击一部番剧即可查看已记录的 RSS 条目，按标题、集数、
简介和状态筛选，并明确选择单条内容加入下载队列。自主下载不会批量补下历史条目，也不会创建
重复任务。

## 显示与无障碍验证

本版完成了 196 个界面状态的自动几何与字体度量复核，覆盖：

- 100%、125% 和 150% 显示缩放；
- 浅色、深色主题；
- 自动最小窗口以及 700、1020、1260、1600 像素宽度；
- 详情默认、展开、有效筛选、无结果、下载列表及设置页顶部/底部。

复核结果为文字裁切 0、表头裁切 0、同级控件重叠 0；详情、下载和设置页没有横向滚动范围。
动画结束后的实时控件始终使用最终布局显示，不依赖被缩放的文字快照。

## 安全的首次刷新

新订阅第一次成功刷新时，AniRSS 默认只记录订阅中已经存在的条目，建立本地基线，不为这些历史
条目创建下载任务。只有在首次刷新前同时开启“自动下载”和“首次刷新时也下载订阅中已有的条目”，
才会下载源中当前列出的既有条目。

关闭自动下载期间记录的条目，以后重新开启时也不会自动补下。用户仍可在订阅详情中对带有下载
地址的条目逐条选择下载；这不会改变安全基线，也不会生成重复任务。

## BT、上传与做种

Windows x86_64 桌面包包含 BT 引擎，不需要另外安装 Python 或 libtorrent。默认设置为下载完成后
不继续做种；用户可以显式开启完成后继续做种，并设置有限的做种分钟数。

该设置只控制下载完成后的行为。BitTorrent 在下载阶段仍可能向其他节点上传数据，并会向 BT
网络暴露用户 IP。HTTP 代理只覆盖 RSS、HTTP 文件和 `.torrent` 元数据请求，不覆盖 Tracker、
DHT、PEX 或节点连接。若内容许可或网络政策禁止再分发，请不要使用 BT。

## 安装包与校验

Windows 发行物是 PyInstaller 目录包。解压后必须同时保留：

```text
AniRSS-0.1.2/
├── AniRSS.exe
└── _internal/
```

`AniRSS.exe` 依赖 `_internal` 中的 Python、Qt、libtorrent 和其他运行文件，不能脱离目录单独复制。
本 Alpha 包尚未进行数字签名，Windows 可能显示未知发布者或 SmartScreen 提示。

最终发布文件的校验值列在 `SHA256SUMS.txt`。PowerShell 示例：

```powershell
Get-FileHash .\AniRSS-0.1.2-windows-x86_64-with-bt.zip -Algorithm SHA256
```

输出必须与校验清单中同名文件的一行一致。不一致或来源不可信时，请不要运行该文件。

## 数据与升级

从 0.1.1 升级到 0.1.2 不需要迁移数据库。关闭旧版本后完整解压新包，再启动新版本即可；不要把
新旧 `_internal` 目录混合覆盖。Windows 默认应用数据目录仍为：

```text
%LOCALAPPDATA%\AniRSS
```

其中 `anirss.db` 保存订阅、已见条目、下载任务和设置，`logs/anirss.log` 保存本地轮转日志。
数据库没有应用层加密，可能包含私人 RSS URL、下载 URL、Token、passkey 和代理配置。公开错误
报告前，请先清理私人地址、凭据、标题、IP 地址和本地用户名。

## Alpha 已知限制

- 不支持 OPML 导入/导出、保存前订阅源测试或独立人工确认候选队列；
- RSS 没有 ETag/Last-Modified 条件请求、自动重试、随机抖动或指数退避；
- HTTP 没有自动重试、限速界面、内容摘要校验或基于 ETag 的断点文件身份校验；
- BT 没有分享率停止、BT/SOCKS 代理、fastresume 或跨任务共享会话；
- 修改并发下载数后，需要重启应用才能保证新并发数完整生效；
- 不提供远程控制、云同步、多用户权限、诊断包导出、内容搜索或 DRM 解密；
- 不自动执行、挂载、解压或安装下载内容；
- 当前正式验证的是 Windows x86_64 桌面发行包；
- 安装包未进行数字签名，界面、设置和数据库结构仍可能在后续 `0.x` 版本中调整。

## 合法使用声明

AniRSS 是通用订阅和下载工具，不提供内容源，不绕过 DRM、付费墙或访问控制，也不授予任何媒体
内容的访问、下载或再分发权。用户只能订阅、下载和分享自己有权访问的内容，并应自行遵守内容
许可、服务条款、网络政策和当地法律。BT 上传可能构成再分发，请特别确认授权范围。

AniRSS 代码以 MIT License 发布；桌面包内的 Python、Qt/PySide6、libtorrent 和其他第三方组件
继续适用各自的许可证与声明。
