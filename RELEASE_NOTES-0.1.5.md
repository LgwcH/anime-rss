# AniRSS 0.1.5 发布说明

发布日期：2026-08-10  
状态：Alpha  
当前可交付物：源码包与 Python wheel

## BT 下载器修复

- 所有种子和磁力任务共享一个应用级 libtorrent session，不再为每个任务重复绑定同一端口；
- 显式启用 DHT、PEX/LSD、UPnP、NAT-PMP、TCP 和 uTP，并加入标准 DHT 引导节点；
- 应用退出时原子保存有限大小的 `libtorrent.state`，下次启动复用 DHT/session 状态；
- 磁力链接默认 120 秒获取不到元数据时失败并释放下载槽；
- 已有元数据但默认 10 分钟没有收到数据时结束本次尝试，用户可以重新排队；
- 暂停任务会取消当前网络尝试并释放线程，继续时建立新 handle 并复用已有文件；
- 进度至少每秒采样一次，避免大型或慢速任务长时间显示零速度；
- 下载列表会显示“正在获取磁力元数据”或“等待可用节点”，不再把所有零速状态笼统显示为下载中。

共享会话已使用 Ubuntu 26.04 官方公开种子的磁力链接完成联网冒烟：取得元数据后立即取消，未保留
ISO 内容。自动化测试不使用用户订阅或私人磁力链接。

## 自启动修复

启动和保存设置时会核对数据库开关、当前用户启动项和完整启动命令。条目被删除、便携目录移动或
升级后路径变化时，会写入当前程序路径；关闭开关仍只删除 AniRSS 自己的当前用户条目。

安全产品隔离 EXE 时仍可能主动删除启动项。自动对账不是杀毒绕过机制，应先处理安全产品的检测
结论并确认文件可信。

## Windows 二进制发布状态

0.1.4 未签名 PyInstaller EXE 被 Microsoft Defender 以 `Trojan:Win32/Bearfoos.A!ml` 隔离。项目
已停止把该文件视为可分发版本。0.1.5 构建脚本要求正式 Windows 发行使用可信 Authenticode 证书、
SHA-256 和时间戳；缺少有效签名时 `-RequireSignature` 会失败。

在微软样本分析给出最终判定并取得可信签名以前，本版本不提供新的官方 Windows EXE。不要关闭
Defender、添加目录排除项或恢复未经复核的 0.1.4 文件。开发者可以从源码运行，或在可信 Python
环境中安装 wheel。

本次更新不修改 SQLite schema；现有订阅文件夹、条目、任务和设置会继续保留。应用重启时原先
处于“正在下载”的任务会回到队列并使用新的共享 BT 引擎。
