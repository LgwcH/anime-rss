# AniRSS 0.1.4 发布说明

发布日期：2026-08-09  
状态：Alpha  
桌面发行包：Windows x86_64，包含 BT/libtorrent

## 本次更新

0.1.4 重新设计了桌面界面，并在不牺牲可读性和操作速度的前提下加入轻量果冻质感：

- 重新划分侧栏、顶栏、工作区、功能卡片、嵌套工具区和数据区的视觉层级；
- 浅色与深色主题使用一致的表面、边框、语义色和选中状态；
- 统计卡加入语义强调色，订阅文件夹工具栏、订阅详情头部和数据表格更容易区分；
- 鼠标点击按钮时提供轻微压缩和弹性回正，开关与侧栏选中胶囊使用短促、可中断的形变反馈；
- 页面切换使用 220ms 的轻量淡入和柔性形变，不排队，不阻塞新页面操作；
- 搜索、筛选、滚动、下载进度和键盘快捷键不播放果冻动画，保持即时响应；
- 自动读取 Windows 动画效果设置，也可使用 `ANIRSS_REDUCE_MOTION=1` 完全关闭附加动效；
- 125%/150% 显示缩放和紧凑窗口下继续保证文字、按钮和表格不重叠、不被遮挡。

## 保留的功能与数据

本次更新不修改数据库 schema。0.1.3 的订阅文件夹、订阅移动、文件夹默认下载目录和滚动位置保持逻辑均继续有效；现有订阅、已记录条目、下载任务和设置会原样保留。

文件夹目录只影响之后新建的下载任务。移动订阅或修改文件夹目录不会搬运磁盘上的已有媒体，也不会修改已经创建的任务。

## 安装与校验

解压 Windows 包后，必须同时保留 `AniRSS.exe` 和 `_internal` 目录。最终发布文件校验值列在 `SHA256SUMS.txt`；本 Alpha 包尚未进行数字签名，Windows 可能显示未知发布者或 SmartScreen 提示。

## 已知限制

- 不支持拖放移动订阅；当前通过选择订阅后点击“移动订阅”完成；
- 不支持文件夹嵌套、OPML 导入/导出或云同步；
- 不会自动整理或搬迁旧目录中的媒体文件；
- 当前正式验证平台为 Windows x86_64。

AniRSS 是通用订阅和下载工具，不提供内容源，也不授予媒体内容的访问、下载或再分发权。请只处理自己有权访问的内容，并遵守内容许可、服务条款、网络政策和当地法律。
