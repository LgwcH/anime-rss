# AniRSS 0.1.6 发布说明

发布日期：2026-09-18  
状态：Alpha  
当前可交付物：Windows x64 打包版（ZIP）、源码包与 Python wheel

## 首个开箱即用的 Windows 打包版

本版本首次在 GitHub Releases 提供 `AniRSS-0.1.6-windows-x64.zip`：解压到任意目录后运行
其中的 `AniRSS.exe` 即可使用，无需安装 Python。压缩包已内置 libtorrent，HTTP、种子和
磁力链接下载开箱可用，并附带许可证、第三方声明和快速开始文档。

**签名与误报提示**：本 EXE 未做 Authenticode 签名（项目尚未取得可信证书）。首次运行时
Windows SmartScreen 可能提示“未知发布者”；0.1.4 的未签名 EXE 曾遭 Microsoft Defender
误报隔离，PyInstaller 打包程序在部分安全软件下仍可能触发类似误报。请只从本仓库的
Releases 页面下载，不要为分发版添加杀毒排除项；如遇查杀请先提交样本分析并等待判定。

## 下载与做种

- BT 做种不再占用并发下载槽位：下载完成的那一刻任务即标记为已完成并让出工作线程，做种
  由后台会话独立管理，达到设定时长后自动停止；重复添加同一种子只刷新做种截止时间。
- 运行中修改并发下载数立即生效，无需重启应用。

## 可靠性修复

- 下载进度与暂停/继续/取消/完成的状态写入改为单列原子更新并按当前状态加守卫，修复并发
  写入可能把“暂停”覆盖回“下载中”或“已取消”的竞态。
- 文件名去重改为按保存目录定向查询，刷新速度不再随任务历史总量增长而下降。
- 长时间运行不再累积已完成或已移除任务的速度采样。

## 网络与 TLS

- RSS 获取、HTTP 下载和种子元数据获取拒绝跳转到非 HTTP(S) 地址以及 HTTPS 降级重定向。
- TLS 不校验模式改用公开 API 实现，不再依赖 `ssl` 私有接口。

## 其他

- 支持范围收紧为 Windows 10/11 x64；非 Windows 平台仍可正常使用 `--help` 与 `--version`。
- 本次更新不修改 SQLite schema；现有订阅、条目、任务和设置完整保留。重启时原先处于
  “正在下载”的任务会回到队列继续。
