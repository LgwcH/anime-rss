# 安全政策

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| 最新发布版本 | 支持 |
| `main` 开发分支 | 尽力支持 |
| 更早版本 | 不支持 |

AniRSS 会处理不受信任的 RSS/XML、HTTP 响应、文件名、种子元数据和磁力链接，因此解析、路径
处理、重定向及下载目录逃逸都属于安全边界。

## 私下报告漏洞

请优先使用托管仓库的 **Security → Report a vulnerability** 私密报告入口。若该入口尚不可用，
请通过项目主页所列的维护者私密联系方式联系，并先询问安全报告渠道；不要在公开 issue、
讨论区或聊天中披露利用细节。

报告中请包含：

- 受影响版本与平台；
- 可复现的最小步骤或概念验证；
- 可能影响，以及你已知的缓解方法；
- 是否已经或计划在其他地方公开。

请去除真实订阅凭据、Cookie、私人 URL 和受版权保护的媒体。维护者会尽快确认收到报告、评估
严重性，并在修复可用后协调披露。处理时间取决于问题复杂度；在双方确认修复前请暂缓公开。

## 安全使用建议

- 只添加你信任或已核验的订阅源；先使用严格过滤规则，并保持“首次刷新时也下载已有条目”关闭。
- 将下载目录设为普通用户可写位置，不要以管理员/root 身份运行 AniRSS。
- 保持 AniRSS、Python、Qt 和 libtorrent 为受支持版本。
- BT 会向网络中的节点暴露 IP 地址；不需要 BT 时不要添加种子/磁力链接，也可不安装可选的
  `libtorrent` 依赖。
- 自动解压和自动执行下载内容不属于 AniRSS 的功能边界。下载完成后请使用系统安全工具检查文件。
- 公开日志前先删除源 URL、查询参数、Cookie、IP 地址、本地用户名和内容标题。

## Windows 杀毒软件检测

正式 Windows 发行版必须具有可验证的 Authenticode 发布者签名，并在签名后生成发布哈希。哈希
只能证明文件未改变，不能证明文件安全。若 Defender 或其他安全产品把发行文件判定为恶意软件：

1. 立即停止分发该二进制，不要要求用户关闭实时保护或添加目录排除项；
2. 保留精确文件哈希、构建依赖清单、检测名称和安全产品版本；
3. 通过安全产品厂商的正式样本提交通道以软件开发者身份请求分析；
4. 只有在厂商给出干净判定、重新签名并完成回归后才恢复发布。

本地未签名 PyInstaller 构建仅供开发验证，不能冒充正式发行包。项目不会使用自修改、混淆、改壳
或重新编译打包器引导程序来规避启发式检测。

## 不属于漏洞的情况

公开 BT 网络固有的 IP 可见性、用户主动添加恶意源后的预期下载，以及不含安全影响的界面问题
通常不视为漏洞，但仍可作为普通问题报告。任何绕过 DRM、付费墙或访问控制的请求均不受支持。
