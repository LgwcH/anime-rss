# AniRSS 0.1.1 发布说明

发布日期：2026-08-07  
状态：Alpha  
桌面发行包：Windows x86_64，包含 BT/libtorrent

AniRSS 是一个本地运行的 RSS/Atom 订阅与下载管理器。它按每个订阅的规则识别新条目，将 HTTP
文件、种子文件或磁力链接交给内置下载器，并保存到自动创建或用户指定的番剧目录。

## 本次发布包含

- RSS/Atom 定时轮询和“立即刷新”；
- 按 GUID 和下载链接去重；
- “必须包含”“排除关键词”和集数提取正则；
- 按订阅自动创建安全化的番剧目录，并处理文件重名；
- 内置 HTTP/HTTPS 下载器，使用 `.part` 文件并支持受校验的断点续传；
- Windows x86_64 包内置 libtorrent，可处理 `.torrent` 和磁力链接；
- 下载队列、进度、速度、暂停、继续、移除和打开保存目录；
- 订阅内容详情、搜索和状态筛选，以及对已记录条目的逐条自主下载；
- 浅色、深色及跟随系统主题，系统托盘和桌面通知；
- 当前用户级开机自启动，不要求管理员权限；
- SQLite 本地持久化、轮转日志和同一数据目录的单实例保护。

## 安全的首次刷新

新订阅第一次成功刷新时，AniRSS 默认只记录订阅中已经存在的条目，建立本地基线，不为这些历史
条目创建下载任务。后续新出现、符合规则且带有可识别下载链接的条目才会自动入队。

只有在首次刷新前同时开启“自动下载”和“首次刷新时也下载订阅中已有的条目”，才会下载源中
当前列出的既有条目。0.1.1 没有独立的人工确认候选队列；关闭自动下载期间已记录的条目，之后重新开启
时也不会自动补下。用户可以打开订阅详情，对带有下载地址的条目逐条明确选择下载；这不会批量
改变安全基线，也不会创建重复任务。

## BT、上传与做种

本次 Windows x86_64 桌面包包含 BT 引擎，不需要另外安装 Python 或 libtorrent。默认设置为下载
完成后不继续做种；用户可以显式开启完成后继续做种，并设置有限的做种分钟数。

该设置只控制下载完成后的行为。BitTorrent 协议在下载阶段仍可能向其他节点上传数据，并会向 BT
网络暴露用户 IP。HTTP 代理设置只覆盖 RSS、HTTP 文件和 `.torrent` 元数据请求，不覆盖 Tracker、
DHT、PEX 或节点连接。若内容许可或网络政策禁止再分发，请不要使用 BT。

## 安装包与完整目录

Windows 发行物是 PyInstaller 目录包。解压后必须同时保留：

```text
AniRSS-0.1.1/
├── AniRSS.exe
└── _internal/
```

`AniRSS.exe` 依赖 `_internal` 中的 Python、Qt、libtorrent 和其他运行文件，不能脱离该目录单独复制
或运行。建议完整解压到固定目录后再创建快捷方式。

本 Alpha 包尚未进行数字签名，Windows 可能显示未知发布者或 SmartScreen 提示。只应从项目正式
发布渠道下载，并在运行前核对 SHA-256。

## SHA-256 校验

最终发布文件的校验值列在与下载文件同目录的 `SHA256SUMS.txt` 中。PowerShell 示例：

```powershell
Get-FileHash .\AniRSS-0.1.1-windows-x86_64-with-bt.zip -Algorithm SHA256
```

输出应与 `SHA256SUMS.txt` 中同名文件的一行完全一致。校验失败、文件名不匹配或来源不可信时，
请删除下载文件，不要启动程序。`SHA256SUMS.txt` 用于发现传输损坏或文件被替换；如果校验清单
本身来自不可信渠道，它不能替代数字签名。

## 数据位置与隐私

Windows 默认应用数据目录为：

```text
%LOCALAPPDATA%\AniRSS
```

其中 `anirss.db` 保存订阅、已见条目、下载任务和设置，`logs/anirss.log` 保存本地轮转日志，
`anirss.instance.lock` 用于阻止两个进程同时打开同一数据目录。媒体保存在用户选择的全局下载目录
或订阅专用目录。

数据库没有应用层加密，可能包含私人 RSS URL、下载 URL、完整标题、Token、passkey 和代理配置。
项目不包含云账户或遥测，也不会自动上传数据库和日志。公开错误报告前，请先清理私人地址、凭据、
标题、IP 地址和本地用户名。

## Alpha 已知限制

- 不支持 OPML 导入/导出、保存前订阅源测试或独立人工确认候选队列；
- RSS 没有 ETag/Last-Modified 条件请求、自动重试、随机抖动或指数退避；
- HTTP 没有自动重试、限速界面、内容摘要校验或基于 ETag 的断点文件身份校验；
- BT 没有分享率停止、BT/SOCKS 代理、fastresume 或跨任务共享会话；
- 修改并发下载数后，需要重启应用才能保证新并发数完整生效；
- 不提供远程控制、云同步、多用户权限、诊断包导出、内容搜索或 DRM 解密；
- 不自动执行、挂载、解压或安装下载内容；
- 仅支持 Windows x86_64，不提供 macOS 或 Linux 版本；
- 安装包未进行数字签名，且界面、设置和数据库结构仍可能在后续 `0.x` 版本中调整。

## 合法使用声明

AniRSS 是通用订阅和下载工具，不提供内容源，不绕过 DRM、付费墙或访问控制，也不授予任何媒体
内容的访问、下载或再分发权。用户只能订阅、下载和分享自己有权访问的内容，并应自行遵守内容
许可、服务条款、网络政策和当地法律。BT 上传可能构成再分发，请特别确认授权范围。

项目代码以 MIT License 发布；随桌面包分发的 Python、Qt/PySide6、libtorrent 和其他第三方组件
继续适用其各自的许可证与声明。
