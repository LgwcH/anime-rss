# AniRSS 0.1.3 发布说明

发布日期：2026-08-09  
状态：Alpha  
桌面发行包：Windows x86_64，包含 BT/libtorrent

## 本次更新

0.1.3 重点改进长列表的使用体验，并为订阅加入可持久化的文件夹管理：

- 修复概览、订阅、下载和设置页面在定时刷新、保存或任务状态变化后跳回顶部的问题；
- 新增订阅文件夹，可分别设置名称和下载根目录；
- 订阅页可以按“全部”“未分类”或指定文件夹筛选；
- 可把选中的订阅移动到其他文件夹，未来下载自动使用目标文件夹下的番剧子目录；
- 新建或编辑订阅时可以直接选择所属文件夹；
- 单个订阅仍可设置自定义保存目录，该设置优先于文件夹默认目录；
- 编辑文件夹的下载根目录后，其中未设置自定义路径的订阅会自动使用新位置；
- 删除文件夹时只删除逻辑分组，其中的订阅转为未分类，已有文件和任务不会删除；
- 文件夹工具栏和订阅表格会在窄窗口隐藏次要列，不产生横向滚动。

## 数据库升级

数据库 schema 从 1 升级到 2。首次启动 0.1.3 时，现有数据库会原地增加订阅文件夹表和订阅关联列；
现有订阅默认保持“未分类”，条目、下载任务和设置不变。建议升级前照常备份：

```text
%LOCALAPPDATA%\AniRSS\anirss.db
```

移动订阅不会搬运磁盘上的已有文件，也不会修改已经创建的下载任务。只有之后新建的任务使用新目录。

## 安装与校验

解压 Windows 包后，必须同时保留 `AniRSS.exe` 和 `_internal` 目录。最终发布文件校验值列在
`SHA256SUMS.txt`；本 Alpha 包尚未进行数字签名，Windows 可能显示未知发布者或 SmartScreen 提示。

## 已知限制

- 不支持拖放移动订阅；当前通过选中订阅后点击“移动订阅”完成；
- 不会自动整理或搬迁旧目录中的媒体文件；
- 不支持文件夹嵌套、OPML 导入/导出或云同步；
- 当前正式验证平台为 Windows x86_64。

AniRSS 是通用订阅和下载工具，不提供内容源，也不授予媒体内容的访问、下载或再分发权。请只处理
自己有权访问的内容，并遵守内容许可、服务条款、网络政策和当地法律。
